import { Oauth2TokenErrorFilter } from './oauth2-token-error.filter';

describe('Oauth2TokenErrorFilter', () => {
  it('should be defined', () => {
    expect(new Oauth2TokenErrorFilter()).toBeDefined();
  });
});
